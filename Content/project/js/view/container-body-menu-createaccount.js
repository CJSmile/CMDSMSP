/*
 * container-body-menu-createaccount
 */

$(document).on('click', cfrmCreateAccount_FirstName, function () {
    disableErrMsg($(this));
});

$(document).on('click', cfrmCreateAccount_LastName, function () {
    disableErrMsg($(this));
});

$(document).on('click', cfrmCreateAccount_Email, function () {
    disableErrMsg($(this));
});

$(document).on('click', cfrmCreateAccount_SecurityCode, function () {
    disableErrMsg($(this));
});

$(document).on('click', cfrmCreateAccount_NewPassword, function () {
    disableErrMsg($(this));
});

$(document).on('click', cfrmCreateAccount_ConfirmPassword, function () {
    disableErrMsg($(this));
});

