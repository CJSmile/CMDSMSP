/*
 * container-body-menu-owner
 */
$(document).on('click', cfrmOwner_OwnerId, function () {
    disableErrMsg($(this));
});

$(document).on('click', cfrmOwner_Password, function () {
    disableErrMsg($(this));
});

$(document).on('click', cfrmOwner_ConfirmPassword, function () {
    disableErrMsg($(this));
});

$(document).on('click', cfrmOwner_FirstName, function () {
    disableErrMsg($(this));
});

$(document).on('click', cfrmOwner_LastName, function () {
    disableErrMsg($(this));
});

$(document).on('click', cfrmOwner_Email, function () {
    disableErrMsg($(this))
});

$(document).on('click', cfrmOwner_Phone, function () {
    disableErrMsg($(this))
});

